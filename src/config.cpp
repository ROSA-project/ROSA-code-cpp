#include "config.hpp"

namespace rosa {

namespace config {

// If set, json will be properly indented to make it more readable (takes more space)
bool prettified_json_output = true;

// void load_config_from_file(const std::string& filename) {
// }

} // namespace config

} // namespace rosa